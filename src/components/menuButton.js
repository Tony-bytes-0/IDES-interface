import {Component} from 'react';

const base = {'display':'flex','width':'20%'}
const button = {'display':'flex'}
class MenuButton extends Component{
    render(){return<>
    <div id='menuButton' style={base}>
        <button style={button} onClick={}></button>
    </div>
    </>

    }
}